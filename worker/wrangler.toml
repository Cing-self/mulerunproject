name = "nano-banana-generator-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# 启用日志和可观测性
[observability]
enabled = true

# 环境变量（非敏感）
[vars]
MULERUN_BASE_URL = "https://api.mulerun.com"
APP_NAME = "nano-banana-generator"
CREATOR_MULTIPLIER = "1.0"

[dev]
port = 8787
